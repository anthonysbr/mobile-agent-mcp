# Roadmap

## v1.0 · shipped

Structured MCP JSON, inline screenshots, `run_flow_with_context`, RN diagnosis, lifecycle tools, flow authoring, `collect_logs`, CLI `--json`.

## v1.1 · next

Example repo + demo GIF, installable agent skill, optional Maestro Cloud wrapper.

## Not planned

Visual diff, device farms, coordinate taps, Rust/Go rewrite.
